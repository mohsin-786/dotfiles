apiVersion: v1
kind: Service
metadata:
  name: voting-svc
  labels:
    name: voting-svc
    app: demo-voting-app
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30001
  selector:
    name: voting-app
    app: demo-voting-app